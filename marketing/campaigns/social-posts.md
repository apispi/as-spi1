---
Purpose: Ready-to-adapt social posts for launches and feature moments.
Channel: social (X/Twitter, LinkedIn)
Placeholders: {{feature_name}}, {{cta_url}}, {{docs_url}}
---

# Social posts

Each post is self-contained. Keep the X/Twitter posts at or under 280 characters after you fill placeholders. Swap `{{feature_name}}` and links per campaign.

## X / Twitter

**1 — capability**
Spi tests more than REST. Send requests over MCP, A2A, gRPC, MQTT and AMQP from one place — then turn the important ones into monitored, asserted suites. {{cta_url}}

**2 — drift**
Three ways an API breaks quietly: an assertion that should fail, a schema that drifts, a value that regresses. Spi watches all three on a schedule and alerts on the first pass→fail. {{cta_url}}

**3 — MCP suite**
Running MCP servers? Spi is a gateway, flight recorder, mock, replay and policy firewall — plus conformance grading and prompt-injection scanning of your tools. {{docs_url}}

**4 — feature drop**
New in Spi: {{feature_name}}. Live in every workspace, nothing to install. {{cta_url}}

## LinkedIn

**1 — problem/approach**
Most API testing stops at REST. But an AI agent stack talks MCP and A2A, your services talk gRPC, your events run over MQTT and AMQP — and each is a place things can break quietly.

Spi tests all of them from one place: send a request, save it into a collection with assertions, then run that collection on a schedule with uptime, latency-trend and drift monitoring. You get alerted on the first pass→fail transition, by email and webhook or Slack.

See how it fits your stack: {{cta_url}}

**2 — feature announcement**
We just shipped {{feature_name}} in Spi.

[INSERT 2–3 SENTENCES: what it does, the concrete problem it removes, who it's for.]

It's available in every workspace now. Details: {{docs_url}}

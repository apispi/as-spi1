// Public documentation repository for Spi. Each entry is one article; `body`
// is an ordered list of content blocks rendered by DocArticle.vue.
export const CATEGORIES = [
  { id: 'getting-started', label: 'Getting started' },
  { id: 'testing', label: 'Testing & assertions' },
  { id: 'automation', label: 'Automation & monitoring' },
  { id: 'mcp', label: 'MCP & agents' },
  { id: 'sharing', label: 'Sharing & reporting' },
  { id: 'developer', label: 'Developer & API' },
];

export const DOCS = [
  {
    slug: 'introduction',
    title: 'What is Spi?',
    category: 'getting-started',
    summary: 'A multi-protocol API testing and monitoring platform for humans and agents.',
    body: [
      { type: 'p', text: 'Spi is a multi-protocol API testing and monitoring platform. You send requests, assert on the responses, chain requests into collections, schedule those collections as monitors, and share the results — all from one place, and all without leaving the browser.' },
      { type: 'p', text: 'What sets Spi apart is breadth. Most tools speak plain HTTP; Spi also speaks the protocols agents and back-ends actually use, and it treats the Model Context Protocol (MCP) as a first-class citizen rather than an afterthought.' },
      { type: 'h2', text: 'Protocols it covers' },
      { type: 'p', text: 'The [Tester](/tester) sends requests over these protocols directly:' },
      { type: 'ul', items: [
        '**REST / HTTP** — any method, headers, query params, and a request body. GraphQL and SOAP are sent this way too: a GraphQL query is a `POST` with a JSON body, and SOAP is a `POST` with an XML body.',
        '**MCP** — the Model Context Protocol, over Streamable HTTP.',
        '**A2A** — Agent-to-Agent, including fetching an agent card.',
        '**gRPC** — unary calls over HTTP/2, with the request message described as JSON.',
        '**MQTT** — publish and subscribe against a broker.',
        '**AMQP** — publish and consume against a broker such as RabbitMQ.',
      ] },
      { type: 'p', text: 'Beyond the Tester, Spi captures **inbound webhooks** at a public URL and runs a full **MCP testing suite** — a gateway, a flight recorder, a mock server, replay, and a policy firewall. See [MCP testing](/docs/mcp-testing).' },
      { type: 'h2', text: 'Who it is for' },
      { type: 'ul', items: [
        'Developers who want a fast, scriptable way to exercise an endpoint and pin down its behaviour.',
        'Teams running scheduled monitors and status pages against production and staging.',
        'Anyone building or integrating AI agents who needs to test, record, mock, and guard MCP servers.',
      ] },
      { type: 'p', text: 'New here? Start with the [quickstart](/docs/quickstart), then learn about [environments and variables](/docs/environments-and-variables) and [assertions](/docs/assertions).' },
      { type: 'note', text: 'Everything you can do in the UI you can also do from a script: see [Programmatic access](/docs/api-keys-and-programmatic-access) and the full [Developer API](/developers).' },
    ],
  },

  {
    slug: 'quickstart',
    title: 'Quickstart',
    category: 'getting-started',
    summary: 'Create an account, send your first request in the Tester, and save it.',
    body: [
      { type: 'p', text: 'This guide takes you from a fresh account to a saved request in a few minutes. All you need is an API endpoint to call — a public one such as `https://httpbin.org/get` works fine.' },
      { type: 'h2', text: '1. Create an account' },
      { type: 'ol', items: [
        'Open the [registration page](/register) and sign up with your email, or use Google sign-in.',
        'Confirm your details to finish registration.',
        'You will land on the [dashboard](/dashboard), which summarises your recent activity.',
      ] },
      { type: 'h2', text: '2. Send your first request' },
      { type: 'ol', items: [
        'Open the [Tester](/tester).',
        'Leave the protocol set to **REST** and the method set to **GET**.',
        'Enter a URL, for example `https://httpbin.org/get`.',
        'Press **Send**. The response — status, timing, headers, and body — appears alongside the request.',
      ] },
      { type: 'p', text: 'Try a request with a body next. Switch the method to **POST** and send some JSON:' },
      { type: 'code', lang: 'json', code: '{\n  "name": "Ada",\n  "role": "engineer"\n}' },
      { type: 'h2', text: '3. Save it' },
      { type: 'p', text: 'Once a request does what you want, save it so you can re-run it, assert on it, or add it to a collection.' },
      { type: 'ol', items: [
        'Give the request a name.',
        'Save it. It now appears in your saved requests and is available to [Collections](/collections).',
      ] },
      { type: 'note', text: 'Values you will reuse — a base URL, a token — belong in an environment as `{{variables}}`, not typed inline. See [Environments and variables](/docs/environments-and-variables).' },
      { type: 'h2', text: 'Where to next' },
      { type: 'ul', items: [
        'Add pass/fail checks with [assertions](/docs/assertions).',
        'Chain saved requests together with [collections](/docs/collections).',
        'Watch an endpoint over time with [monitors and alerts](/docs/monitors-and-alerts).',
      ] },
    ],
  },

  {
    slug: 'sending-requests',
    title: 'Sending requests',
    category: 'testing',
    summary: 'The Tester in depth: protocols, headers, body, params, and reading responses.',
    body: [
      { type: 'p', text: 'The [Tester](/tester) is where you compose and send a single request over any supported protocol and inspect exactly what came back. This article covers each part of the request panel.' },
      { type: 'h2', text: 'Choosing a protocol' },
      { type: 'p', text: 'The protocol selector changes what the rest of the panel offers. Spi supports these protocols directly:' },
      { type: 'ul', items: [
        '**REST** — pick a method (`GET`, `POST`, `PUT`, `PATCH`, `DELETE`, and so on), set a URL, and optionally add headers, query params, and a body.',
        '**MCP** — point at an MCP server endpoint (Streamable HTTP) and call its tools, prompts, and resources.',
        '**A2A** — call an Agent-to-Agent endpoint, or fetch its `agent-card`.',
        '**gRPC** — a unary call over HTTP/2. Describe the request message as a JSON array of fields, e.g. `[{"field":1,"type":"string","value":"world"}]`. Toggle TLS for HTTP/2 over TLS.',
        '**MQTT** — publish or subscribe against a broker, with a topic and message.',
        '**AMQP** — publish or consume against a broker, with a queue or exchange and a message.',
      ] },
      { type: 'note', text: 'There is no separate GraphQL or SOAP protocol: a GraphQL request is a REST `POST` with a JSON body (`{"query": "..."}`), and SOAP is a REST `POST` with an XML body and the appropriate content-type header.' },
      { type: 'h2', text: 'Headers' },
      { type: 'p', text: 'Add request headers as name/value rows. For gRPC these become call **metadata**. Header names and values can both contain `{{variables}}`.' },
      { type: 'code', lang: 'http', code: 'Authorization: Bearer {{token}}\nContent-Type: application/json' },
      { type: 'h2', text: 'Query params' },
      { type: 'p', text: 'For REST requests, add query parameters as rows; Spi appends them to the URL. You can also type them directly into the URL — either works.' },
      { type: 'h2', text: 'Request body' },
      { type: 'p', text: 'The body field is labelled to match the protocol — **Body** for REST, **Request** for gRPC, **Message** for MQTT and AMQP. For REST, send JSON, XML, form data, or plain text; set the matching `Content-Type` header.' },
      { type: 'h2', text: 'Reading the response' },
      { type: 'p', text: 'Every response shows:' },
      { type: 'ul', items: [
        '**Status** — the HTTP status code (or protocol-specific outcome).',
        '**Time** — how long the request took, in milliseconds.',
        '**Headers** — the response headers.',
        '**Body** — the response payload, pretty-printed when it is JSON.',
      ] },
      { type: 'p', text: 'Requests you send are kept in your history so you can revisit them. When you are happy with a request, save it — saved requests can carry [assertions](/docs/assertions) and a [response contract](/docs/response-contracts), and can be added to [collections](/docs/collections).' },
      { type: 'note', text: 'Spi guards outbound requests against SSRF, so requests to internal or loopback addresses may be refused. This protects the shared infrastructure the Tester runs on.' },
    ],
  },

  {
    slug: 'environments-and-variables',
    title: 'Environments and variables',
    category: 'testing',
    summary: 'Reuse values with {{variable}} placeholders, a default environment, and secret masking.',
    body: [
      { type: 'p', text: 'An environment is a named set of variables. Instead of hard-coding a base URL or a token into every request, you reference it as `{{name}}` and let the selected environment supply the value. Switching environments — say from staging to production — re-points every request at once.' },
      { type: 'h2', text: 'The {{variable}} syntax' },
      { type: 'p', text: 'Anywhere in a request — the URL, header names and values, query params, and the body — write `{{variable_name}}`. Just before the request is sent, Spi substitutes each placeholder with the value from the active environment.' },
      { type: 'code', lang: 'http', code: 'GET {{base_url}}/users/{{user_id}}\nAuthorization: Bearer {{token}}' },
      { type: 'p', text: 'Variable names may contain letters, digits, underscores, dots, and hyphens. A few rules keep substitution predictable:' },
      { type: 'ul', items: [
        'Substitution is a **single pass** — a value that itself contains `{{...}}` is not expanded again.',
        'An **unknown** placeholder is left in place and reported as unresolved, so a typo shows up as an obviously-broken request rather than a silently empty one.',
        'Only `{{name}}` patterns are touched, so JSON bodies that legitimately contain braces are left alone.',
      ] },
      { type: 'h2', text: 'Managing environments' },
      { type: 'ol', items: [
        'Create an environment and give it a name (for example, "Staging").',
        'Add variables as key/value pairs.',
        'Select it as the active environment when you send requests or run collections.',
      ] },
      { type: 'h2', text: 'The default environment' },
      { type: 'p', text: 'One environment can be flagged as the **default**. It is the one used when nothing else is chosen — for example the fallback for a new request. Marking a different environment as default moves that flag.' },
      { type: 'h2', text: 'Secrets and masking' },
      { type: 'p', text: 'Flag a variable as **secret** when its value is a credential. Secret handling is built in:' },
      { type: 'ul', items: [
        'Secret values are **never sent back** to the browser by the API — the field returns empty, and the "secret" flag is preserved.',
        'Wherever Spi persists or echoes a resolved request — request history, and any run that becomes a shareable report — the secret value is replaced with a mask (`••••••`).',
        'Very short values (under four characters) are not masked, because blanking every "ok" or "1" in a response would destroy the output for no real protection.',
      ] },
      { type: 'note', text: 'Because secrets are masked in [shared reports](/docs/reports-and-sharing), you can share a collection run without leaking the token it used.' },
    ],
  },

  {
    slug: 'assertions',
    title: 'Assertions',
    category: 'testing',
    summary: 'The closed operator vocabulary, how assertions pass and fail, and examples.',
    body: [
      { type: 'p', text: 'An assertion is a single automated check against a response: pick a **path** into the response, an **operator**, and (for most operators) an **expected** value. A saved request can carry a list of assertions; when it runs, every assertion is evaluated and the request passes only if all of them pass.' },
      { type: 'h2', text: 'What a path can point at' },
      { type: 'p', text: 'A path names the value to check:' },
      { type: 'ul', items: [
        '`status` — the response status code.',
        '`time_ms` — the elapsed time in milliseconds.',
        '`header.<name>` — a response header, matched case-insensitively (e.g. `header.content-type`).',
        'A dot path into the decoded JSON body, e.g. `data.items.0.id`. A leading `$.` is optional and array indices may be written `items[0]` or `items.0`.',
        '`$` (or an empty path) — the decoded body itself, for asserting on the shape of a top-level array or object.',
        '`body` — the raw body as a whole, useful when the response is not JSON.',
      ] },
      { type: 'h2', text: 'The operator vocabulary' },
      { type: 'p', text: 'The set of operators is **closed** and deliberately small — an open vocabulary where "contains", "includes", and "has" all mean the same thing is something no evaluator can act on. These are the only operators:' },
      { type: 'ul', items: [
        '`equals` / `not_equals` — equality. Comparison is loose on purpose, so an expected `"200"` matches an integer status of `200`.',
        '`contains` / `not_contains` — substring (or JSON-encoded substring) test.',
        '`matches` — a regular expression. Delimiters are optional: both `/foo/i` and a bare `foo` work.',
        '`exists` / `not_exists` — whether a value is present at the path. These take no expected value.',
        '`greater_than` / `greater_or_equal` / `less_than` / `less_or_equal` — numeric comparison.',
        '`is_type` — the JSON type of the value; expected is one of `string`, `number`, `boolean`, `array`, `object`, `null`.',
        '`has_length` — the length of a string, or the element count of an array or object.',
      ] },
      { type: 'note', text: 'Every operator except `exists` and `not_exists` needs an expected value. `exists` and `not_exists` are the only ones that treat a missing value as anything other than a failure.' },
      { type: 'h2', text: 'How pass and fail work' },
      { type: 'p', text: 'Each assertion reports its own result — the actual value found, whether it passed, and an error reason when it could not be evaluated (an unknown operator, a missing value, an invalid regex). Evaluation never throws: one malformed assertion is reported, not allowed to explode the whole suite. The request passes only when every assertion passes.' },
      { type: 'h2', text: 'Examples' },
      { type: 'code', lang: 'text', code: 'status            equals            200\nheader.content-type  contains       application/json\ndata.items        is_type           array\ndata.items        has_length        10\ndata.items.0.id   exists\ntime_ms           less_than         500\nemail             matches           ^[^@]+@[^@]+$' },
      { type: 'p', text: 'Assertions are also enforced during a [collection run](/docs/collections) and are what a [monitor](/docs/monitors-and-alerts) checks on every scheduled run. For catching silent schema changes no assertion was written for, pair them with a [response contract](/docs/response-contracts).' },
    ],
  },

  {
    slug: 'response-contracts',
    title: 'Response contracts',
    category: 'testing',
    summary: 'Capture an inferred response schema and catch breaking vs additive drift automatically.',
    body: [
      { type: 'p', text: 'A response contract catches the changes assertions miss. You never write the schema — Spi infers a compact structural description from a known-good response, stores it on the saved request, and checks later responses against it. A silently dropped field or a number that became a string is caught even when the status stays `200`.' },
      { type: 'h2', text: 'What the contract captures' },
      { type: 'p', text: 'From a decoded JSON response, Spi records:' },
      { type: 'ul', items: [
        'The **type** of every value (`string`, `integer`, `number`, `boolean`, `object`, `array`, `null`).',
        'For objects, the **properties** and which of them were **required** (present in the sample).',
        'For arrays, a single merged **item schema** describing the element shape.',
        'A recognisable **format** on strings — `date-time`, `email`, `uri`, or `uuid` — because a date field that stops being a date is a real break.',
      ] },
      { type: 'h2', text: 'Breaking vs additive drift' },
      { type: 'p', text: 'When a live response is checked against the contract, Spi classifies each difference into three kinds — and only two of them break a consumer:' },
      { type: 'ul', items: [
        '**Removed** (breaking) — a field the contract marked required is gone.',
        '**Type changed** (breaking) — a field is no longer the type the contract recorded. Note that an `integer` still satisfies a `number`.',
        '**Added** (additive) — a field appeared that the contract never described. This is reported but does not break anything.',
      ] },
      { type: 'p', text: 'Every difference is reported with a dotted path (e.g. `$.data.items[0].id`) so a drift report points at the exact field. A response conforms only when there is no breaking drift and nothing was added.' },
      { type: 'h2', text: 'Capturing and checking a contract' },
      { type: 'ol', items: [
        'Send the request in the [Tester](/tester) until you get a representative, known-good JSON response.',
        'Infer and attach the contract to the saved request — Spi walks the response into a schema for you.',
        'Later, check a fresh response against the stored contract to see what drifted.',
      ] },
      { type: 'note', text: 'During a [collection run](/docs/collections), a step with a contract attached is checked automatically: **breaking** drift fails the step (catching silent breaks no assertion covered), while **additive** drift is reported without failing. A [monitor](/docs/monitors-and-alerts) therefore alerts you the moment an endpoint breaks its shape.' },
    ],
  },

  {
    slug: 'collections',
    title: 'Collections',
    category: 'automation',
    summary: 'Chain saved requests into ordered steps, thread variables between them, and run the lot.',
    body: [
      { type: 'p', text: 'A collection is an ordered sequence of saved requests, run start to finish against one environment. It is how you model a real flow — log in, create a record, fetch it, delete it — and get a single pass/fail for the whole thing. Manage them in [Collections](/collections).' },
      { type: 'h2', text: 'Steps and ordering' },
      { type: 'p', text: 'Each step wraps a saved request. Steps run in order, top to bottom, so you can arrange them into a dependency chain. The request that each step sends is resolved against the current variable pool immediately before it is sent — which is what makes variable threading (below) work.' },
      { type: 'h2', text: 'The variable pool and extraction' },
      { type: 'p', text: 'A run starts with the selected environment\'s variables as its pool. A step can then **extract** values from its response and add them to the pool, so a later step can use them. That is how a login step hands a token to every step after it.' },
      { type: 'p', text: 'An extraction rule is a **name** plus a **path** into the response — the same path forms assertions use: a dot path into the JSON body, or `status`, `time_ms`, or `header.<name>`. The value is stored under the name and becomes available as `{{name}}` in subsequent steps.' },
      { type: 'code', lang: 'text', code: 'Step 1  POST /login       extract  token  <-  data.access_token\nStep 2  GET  /me           header   Authorization: Bearer {{token}}' },
      { type: 'note', text: 'Only scalar values (strings, numbers, booleans) can be extracted, because only a scalar can be substituted back into a later request. A path that resolves to an object or array is skipped.' },
      { type: 'h2', text: 'Assertions and contracts per step' },
      { type: 'p', text: 'Each step is evaluated with the [assertions](/docs/assertions) and [response contract](/docs/response-contracts) attached to its saved request. A step passes when the response is OK, its assertions all pass, and there is no breaking contract drift.' },
      { type: 'h2', text: 'Continue on failure' },
      { type: 'p', text: 'By default a collection **stops** at the first failing step, and the remaining steps are reported as skipped rather than silently dropped. Turn on **continue on failure** to run every step regardless — useful for an independent suite of checks where one failure should not hide the others.' },
      { type: 'h2', text: 'Running a collection' },
      { type: 'p', text: 'Run a collection from the UI and you get a full report: total, passed, failed, and skipped counts, the elapsed time, and per-step detail including the resolved URL (with secrets masked), status, timing, assertion results, contract drift, and which variables it extracted.' },
      { type: 'note', text: 'A collection with no steps is reported as a failure, not a pass — a monitor running an empty collection should not report green while checking nothing.' },
      { type: 'p', text: 'Collections can be run automatically by a [monitor](/docs/monitors-and-alerts), triggered by an inbound [webhook](/docs/webhooks), run over a [dataset](/docs/webhooks), or run from CI over the [programmatic API](/docs/api-keys-and-programmatic-access). They can also be exported to Postman or OpenAPI — see [Reports and sharing](/docs/reports-and-sharing).' },
    ],
  },

  {
    slug: 'monitors-and-alerts',
    title: 'Monitors and alerts',
    category: 'automation',
    summary: 'Schedule a collection, track uptime, and get alerted when status changes.',
    body: [
      { type: 'p', text: 'A monitor runs a [collection](/docs/collections) on a schedule, records each run as a history point, and alerts you when its pass/fail status changes. It is how you turn a suite of checks into continuous confidence that an API is healthy. Manage monitors in [Monitors](/monitors).' },
      { type: 'h2', text: 'Setting up a monitor' },
      { type: 'ol', items: [
        'Pick the collection to run and the environment to run it against.',
        'Choose how often it should run.',
        'Enable alerts and choose the channels to notify.',
      ] },
      { type: 'p', text: 'Each run is stored as an inspection report and a lightweight history entry (passed/failed, timing, and how many steps passed). Older history is trimmed automatically so a monitor keeps a rolling window rather than growing forever.' },
      { type: 'h2', text: 'Alert on transition, not on repeat' },
      { type: 'p', text: 'Alerts fire only when the status **changes** — passing to failing, and failing back to passing. A monitor that emailed every five minutes while an API was down would get muted, and then it would not be a monitor any more. The first run establishes a baseline and does not announce an outage or a recovery.' },
      { type: 'h2', text: 'Alert channels' },
      { type: 'p', text: 'Alerts go to your email plus any enabled alert channels. A channel is a reusable destination, shared across all your monitors. Supported channel types:' },
      { type: 'ul', items: [
        '**Slack** — an incoming-webhook URL; the alert is posted as a message.',
        '**Discord** — an incoming-webhook URL; the alert is posted as a message.',
        '**Webhook** — a generic URL that receives a structured JSON payload describing the event.',
      ] },
      { type: 'note', text: 'Webhook, Slack, and Discord channels work with no SMTP configured at all, and one failing destination never stops the others. Channel URLs are credentials — anyone holding one can post to your channel — so they are treated as secrets.' },
      { type: 'h2', text: 'MCP drift monitors' },
      { type: 'p', text: 'A monitor can instead watch an MCP server for **drift**: it snapshots the server\'s tool list and compares each run against the previous snapshot. When the surface changes it records a failing result and alerts once; the new shape then becomes the baseline, so it does not re-alert forever or send a misleading "recovered" message. See [MCP testing](/docs/mcp-testing).' },
      { type: 'h2', text: 'Uptime and status' },
      { type: 'p', text: 'A monitor\'s history is the raw material for a public [status page](/docs/reports-and-sharing), which renders current status and recent uptime from these results at a shareable URL.' },
    ],
  },

  {
    slug: 'webhooks',
    title: 'Webhooks, datasets, and profiling',
    category: 'automation',
    summary: 'Capture inbound webhooks, trigger collection runs from events, run datasets, and profile performance.',
    body: [
      { type: 'p', text: 'Not every test starts with you pressing Send. Spi can receive inbound HTTP callbacks, react to them by running a collection, iterate a collection over a dataset, and profile how a single request performs under load. Manage inbound endpoints in [Webhooks](/webhooks).' },
      { type: 'h2', text: 'Capturing inbound webhooks' },
      { type: 'p', text: 'Create a webhook endpoint and Spi gives you a public capture URL:' },
      { type: 'code', lang: 'text', code: 'https://apispi.com/hook/{token}' },
      { type: 'p', text: 'Any HTTP method to that URL is captured. It is unauthenticated by design — the token is the credential, exactly like a Slack incoming-webhook URL — and it always answers quickly without revealing whether a token exists.' },
      { type: 'h3', text: 'Inspecting captures' },
      { type: 'p', text: 'Each capture records the method, headers, query string, body, and source IP. A few safeguards apply:' },
      { type: 'ul', items: [
        'Sensitive headers (`authorization`, `cookie`, `x-api-key`, `proxy-authorization`) are stored as a mask, never verbatim.',
        'Bodies larger than 64 KB are truncated, not rejected, and flagged as truncated.',
        'Old captures are trimmed so an endpoint keeps a rolling window.',
      ] },
      { type: 'h3', text: 'Silence detection' },
      { type: 'p', text: 'An endpoint can expect a request at least every N minutes. If nothing arrives it goes **silent** and alerts you; when a request arrives again it reports the recovery — over the same [alert channels](/docs/monitors-and-alerts) your monitors use.' },
      { type: 'h2', text: 'Webhook-triggered collection runs' },
      { type: 'p', text: 'An endpoint can be wired to **trigger a collection run** whenever it receives a request. This is event-driven testing: a provider\'s callback kicks off your suite. The run happens off the request path, so the provider still gets an immediate response.' },
      { type: 'p', text: 'Top-level scalar fields of a JSON payload are handed to the run as variables named `webhook_<key>` (plus `webhook_method`), so the suite can target the exact record the callback named. For a payload of `{"order_id": 42}`, the run can reference `{{webhook_order_id}}`.' },
      { type: 'h2', text: 'Data-driven (dataset) runs' },
      { type: 'p', text: 'Instead of running a collection once, run it once **per row** of a dataset. Each row supplies a set of variables that override the environment for that iteration, so one collection covers many cases — different users, inputs, or edge cases — and you get a pass/fail per row.' },
      { type: 'h2', text: 'Performance profiling' },
      { type: 'p', text: 'A saved request can be profiled by sending it repeatedly and summarising the timings — latency percentiles and throughput — so you can see how an endpoint behaves under repeated load, separate from correctness checks.' },
      { type: 'note', text: 'Triggered runs, dataset runs, and profiling are throttled like other outbound tests to protect both Spi and the targets you point them at.' },
    ],
  },

  {
    slug: 'mcp-testing',
    title: 'MCP testing',
    category: 'mcp',
    summary: 'The full MCP suite: gateway, flight recorder, mock server, replay, firewall, and agent explorer.',
    body: [
      { type: 'p', text: 'Spi treats the Model Context Protocol as a first-class testing target and gives it a suite of its own. You can call MCP servers, be an MCP server, record and mock real traffic, replay it for regressions, guard it with a policy firewall, and let an agent explore a server autonomously.' },
      { type: 'h2', text: 'Calling MCP servers (the Tester)' },
      { type: 'p', text: 'In the [Tester](/tester), choose the **MCP** protocol, point at a server\'s Streamable HTTP endpoint, and call its tools, prompts, and resources — asserting on the results just like any other response.' },
      { type: 'h2', text: 'The gateway — Spi as an MCP server' },
      { type: 'p', text: 'The inversion at the heart of the suite: every other MCP server is something agents test *with*; the gateway makes Spi the testing tool agents *use*. It is a real MCP server at:' },
      { type: 'code', lang: 'text', code: 'POST https://apispi.com/api/gateway/tools' },
      { type: 'p', text: 'It speaks JSON-RPC 2.0 over Streamable HTTP, is stateless (no session is issued), and is authenticated with the same personal API key as the [programmatic API](/docs/api-keys-and-programmatic-access) — so an agent is scoped to its owner\'s data. Its tools are your own artefacts: run a collection, check monitors, evaluate assertions, send a guarded HTTP request.' },
      { type: 'h2', text: 'Flight recorder and relay' },
      { type: 'p', text: 'The flight recorder is a relay that sits between an agent and an upstream MCP server. Configure a proxy and Spi gives you a relay URL:' },
      { type: 'code', lang: 'text', code: 'https://apispi.com/mcp-proxy/{token}' },
      { type: 'p', text: 'Point your agent at that URL instead of the real server. Spi forwards each exchange upstream, records it, and returns the response — so you get a full transcript of the MCP traffic your agent actually generated. Recorded exchanges can be inspected, turned into a mock, or replayed.' },
      { type: 'h2', text: 'Mock server' },
      { type: 'p', text: 'Spi can serve a mock MCP server so you can develop against a stable, offline stand-in. Its URL is:' },
      { type: 'code', lang: 'text', code: 'https://apispi.com/mcp-mock/{token}' },
      { type: 'p', text: 'Like the capture endpoints it is unauthenticated — the token is the credential. You can build a mock by hand, or generate one **from recorded traffic** so the mock mirrors a real server\'s responses.' },
      { type: 'h2', text: 'Replay for regressions' },
      { type: 'p', text: 'Replay takes a flight recorder\'s captured requests and re-runs them against a target server, diffing each new response against what was originally recorded — "record production traffic once, replay it against staging to catch regressions". A response whose shape changed (a dropped field, a changed type) is surfaced using the same [contract](/docs/response-contracts) engine. It is safe by default: a recorded call to a destructive-looking tool is skipped rather than re-executed, so replaying against a live server does not repeat side effects.' },
      { type: 'h2', text: 'Firewall and policy' },
      { type: 'p', text: 'The relay can enforce an ordered **policy** on the traffic flowing through it — an MCP firewall. Each rule has an action and a direction:' },
      { type: 'ul', items: [
        '**action** — `block` (stop the call) or `redact` (mask matching values).',
        '**direction** — `request` (the agent\'s call) or `response` (the server\'s reply).',
        '**tool** — an optional regex on the tool name (request side).',
        '**pattern** — a regex; for `redact` it selects keys/values to mask, for a response `block` it triggers when the response text matches.',
        '**on_injection** — a response rule that triggers when the built-in injection scanner flags the reply.',
      ] },
      { type: 'p', text: 'Rules apply in order: the first `block` wins and stops evaluation, while redactions accumulate. This lets you stop dangerous tool calls and strip secrets out of traffic inline.' },
      { type: 'h2', text: 'Agent explorer' },
      { type: 'p', text: 'The agent explorer turns the agent loop into a discovery and red-team instrument: an AI model pursues a goal by calling a server\'s tools, and Spi records the path it took and the capabilities it found. In **safe mode** (the default) it refuses side-effecting tools, so pointing it at a real server is safe — a blocked destructive call is recorded as a *finding*, not an execution — and the discovered tool surface is run through the injection scanner as it goes.' },
      { type: 'note', text: 'To watch an MCP server\'s tool surface over time, set up an [MCP drift monitor](/docs/monitors-and-alerts).' },
    ],
  },

  {
    slug: 'reports-and-sharing',
    title: 'Reports and sharing',
    category: 'sharing',
    summary: 'Inspection reports, shareable links, public status pages, and Postman/OpenAPI export.',
    body: [
      { type: 'p', text: 'Runs in Spi produce inspection reports you can keep, compare, and — when you choose — share publicly with an unguessable link. You can also publish a status page and export collections to other tools. Browse saved reports in [Reports](/reports).' },
      { type: 'h2', text: 'Inspection reports' },
      { type: 'p', text: 'A collection run, a monitor run, and other inspections are saved as reports with a summary and the full result data. You can list them, open one for detail, delete them, and compare two to see what changed between runs.' },
      { type: 'h2', text: 'Shareable report links' },
      { type: 'p', text: 'Share a report and Spi mints an unguessable token, giving you a public, read-only URL:' },
      { type: 'code', lang: 'text', code: 'https://apispi.com/r/{token}' },
      { type: 'p', text: 'Anyone with the link can view the report without an account. You can revoke sharing at any time, which invalidates the link. Because secrets are masked wherever a run is persisted, a shared report will not leak the tokens the run used — see [Environments and variables](/docs/environments-and-variables).' },
      { type: 'h2', text: 'Public status pages' },
      { type: 'p', text: 'A status page publishes the health of one or more [monitors](/docs/monitors-and-alerts) at a public URL:' },
      { type: 'code', lang: 'text', code: 'https://apispi.com/status/{token}' },
      { type: 'p', text: 'It is token-gated and unauthenticated, renders current status and recent uptime from monitor history, and can be enabled or disabled. It is the page you point customers or teammates at instead of answering "is it down?" by hand.' },
      { type: 'h2', text: 'Exporting collections' },
      { type: 'p', text: 'A [collection](/docs/collections) can be exported so it lives on outside Spi:' },
      { type: 'ul', items: [
        '**Postman** — export as a Postman collection to run or share it in Postman.',
        '**OpenAPI 3.1** — export the REST/HTTP requests as an OpenAPI 3.1 document. Spi\'s `{{var}}` templating maps to OpenAPI\'s single-brace `{var}` path/server variables, and attached response contracts become response schemas.',
      ] },
      { type: 'note', text: 'OpenAPI export covers REST/HTTP requests, since the other protocols have no OpenAPI representation. Spi can also import: bring in a `curl` command or an OpenAPI document to create requests.' },
    ],
  },

  {
    slug: 'api-keys-and-programmatic-access',
    title: 'API keys and programmatic access',
    category: 'developer',
    summary: 'Create named API keys and drive the /api/v1 endpoints from scripts and CI.',
    body: [
      { type: 'p', text: 'Everything you do in the UI you can also do from a script. The programmatic API lives under `/api/v1`, is stateless, and is authenticated with a personal API key sent as a bearer token. Manage your keys in [Profile](/profile).' },
      { type: 'h2', text: 'Named API keys' },
      { type: 'p', text: 'API keys are named personal credentials — you can hold several at once and name them for where they are used (for example, "CI" or "Local script").' },
      { type: 'ul', items: [
        'Keys are prefixed `spi_` so they are recognisable in logs and config.',
        'You can create **multiple** keys per user, up to a fixed limit — if you hit it, revoke one first.',
        'A key can be given an optional **expiry** date, after which it stops working.',
        'The full key value is shown **exactly once**, at creation — copy it then, because it cannot be retrieved later.',
        'Keys are **revoked**, not deleted, so their last-used history and audit trail remain; a revoked key stops working immediately.',
      ] },
      { type: 'h2', text: 'Authenticating requests' },
      { type: 'p', text: 'Send the key as a bearer token on every request to `/api/v1`:' },
      { type: 'code', lang: 'http', code: 'Authorization: Bearer spi_your_key_here' },
      { type: 'p', text: 'These routes are separate from the session-authenticated routes the web app uses: no cookies means no CSRF concern.' },
      { type: 'h2', text: 'A curl example' },
      { type: 'p', text: 'Send an HTTP request through Spi\'s guarded proxy:' },
      { type: 'code', lang: 'bash', code: 'curl -X POST https://apispi.com/api/v1/proxy \\\n  -H "Authorization: Bearer spi_your_key_here" \\\n  -H "Content-Type: application/json" \\\n  -d \'{\n    "method": "GET",\n    "url": "https://httpbin.org/get"\n  }\'' },
      { type: 'h2', text: 'Running a collection from CI' },
      { type: 'p', text: 'Run a collection by id and gate on the status code alone — `200` when every step passed, `422` when any failed:' },
      { type: 'code', lang: 'bash', code: 'curl -X POST https://apispi.com/api/v1/collections/{id}/run \\\n  -H "Authorization: Bearer spi_your_key_here"' },
      { type: 'p', text: 'The same key also authenticates the [MCP gateway](/docs/mcp-testing), so an agent driving Spi is scoped to your data.' },
      { type: 'note', text: 'The `/api/v1` surface covers the proxy, the per-protocol testers (MCP, A2A, gRPC, MQTT, AMQP), and collection runs. For the complete, always-current endpoint reference — every field and response shape — see the [Developer API](/developers) page.' },
    ],
  },
];
